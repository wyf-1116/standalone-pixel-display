version: '3.8'

services:
  pixel-display:
    build: .
    container_name: standalone-pixel-display
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./apps:/app/apps
